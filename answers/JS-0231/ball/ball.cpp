#include <bits/stdc++.h>

using namespace std;

int main(){
	freopen("ball.in", "r", stdin);freopen("ball.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(0);cout.tie(0);
	int n, m;
	cin >> n >> m;
	cout << n * m << endl;
	cout << "AFO." << endl;
	return 0;
}

