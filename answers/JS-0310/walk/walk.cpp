#include<bits/stdc++.h>
using namespace std;

int n,k,b,c,w[500050];

int main(){
	freopen("walk.in","r",stdin);
	freopen("walk.out","w",stdout);
	cin>>n>>k;
	for(int i=1;i<=k;++i)
		cin>>w[i];
	for(int i=1;i<=n;++i)
		cin>>b>>c;
	cout<<-1<<endl;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
