#include<bits/stdc++.h>
using namespace std;
int n,k;
int w[15];
pair<int,int> s[600000];
int main(){
	freopen("walk.in","r",stdin);
	freopen("walk.out","w",stdout);
	cin>>n>>k;
	for(int i=0;i<k;i++)scanf("%d",&w[i]);
	for(int i=0;i<n;i++){
		int x,y;scanf("%d%d",&y,&x);
	}
	cout<<-1<<endl;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
